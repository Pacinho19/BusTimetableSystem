spring:
  jpa:
    hibernate:
      ddl-auto: validate

  h2:
    console:
      enabled: true
      path: '/console'

  datasource:
    url: 'jdbc:h2:mem:mydb'
    username: 'sa'
    password: ''
    driverClassName: 'org.h2.Driver'

  flyway:
    baselineOnMigrate: true